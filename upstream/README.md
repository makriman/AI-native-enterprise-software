# Upstream Baseline

`upstream/odoo-community` is a pinned Odoo Community baseline checkout.

Pin metadata lives in `upstream/odoo-community.lock`.

Do not edit upstream files directly unless an explicit `elevated_core_patch` process is approved.
