{
  "name": "@oae/runner-supervisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'runner-supervisor lint: pending full ruleset'",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.3.2",
    "uuid": "^11.1.0",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
