{
  "name": "@oae/spec-compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/spec-compiler/src/index.js",
  "types": "dist/spec-compiler/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'spec-compiler lint: pending full ruleset'",
    "test": "node --test"
  },
  "dependencies": {
    "@oae/spec-schema": "workspace:*"
  }
}
