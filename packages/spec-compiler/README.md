# @oae/spec-compiler

Deterministic compiler that turns canonical customization specs into:

- baseline Odoo module skeletons (`addons/generated`)
- mandatory build artifacts (`build-manifest.json`, risk report, summaries)
- test and documentation stubs for Codex refinement
