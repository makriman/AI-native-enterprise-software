{
  "name": "@oae/deployment",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/deployment/src/index.js",
  "types": "dist/deployment/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'deployment lint: pending full ruleset'",
    "test": "node --test"
  },
  "dependencies": {
    "@oae/shared-types": "workspace:*"
  }
}
